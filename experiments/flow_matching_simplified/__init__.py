"""Flow Matching with Variable-Length Paired Token Breakpoints.

This package implements flow matching models for generating soil profiles
using variable-length paired token breakpoints: [ts_value, depth_value] per layer.
"""

__version__ = "0.1.0"

all = ["data", "model"]
